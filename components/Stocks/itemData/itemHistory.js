import { StyleSheet, Text, View, Image, Pressable, TextInput } from 'react-native';


export default ItemHistory = () => {

    return(
        <Text style={styles.historyConatiner} >
            Work in Progress
        </Text>
    );

};

const styles = StyleSheet.create({

    historyConatiner:{
        alignItems: "center",
        justifyContent: "center",
    }

});